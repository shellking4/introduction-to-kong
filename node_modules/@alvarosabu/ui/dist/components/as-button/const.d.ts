import { DynamicShortcut } from 'unocss';
export type ButtonType = 'button' | 'submit' | 'reset';
export type ButtonVariant = 'primary' | 'secondary' | 'tertiary';
export type ButtonSize = 'default' | 'block' | 'sm' | 'md' | 'lg';
export declare enum btnType {
    BUTTON = "button",
    SUBMIT = "submit",
    RESET = "reset"
}
export declare enum btnVariant {
    PRIMARY = "primary",
    SECONDARY = "secondary"
}
export declare enum btnSize {
    DEFAULT = "default",
    BLOCK = "block",
    SM = "sm",
    MD = "md",
    LG = "lg"
}
export declare const btnShortcuts: DynamicShortcut[];
export declare const btnSafelist: string[];
export declare const btnVariantList: btnVariant[];
export interface ButtonProps {
    /**
     * Represents the properties of a button component.
     */
    label?: string;
    icon?: string;
    iconRight?: string;
    disabled?: boolean;
    outline?: boolean;
    link?: boolean;
    target?: string;
    variant?: string;
    size?: string;
    flat?: boolean;
    transparent?: boolean;
}
declare const AsBtnConsts: {
    btnType: typeof btnType;
    btnVariant: typeof btnVariant;
    btnSize: typeof btnSize;
    btnShortcuts: DynamicShortcut[];
    btnSafelist: string[];
    btnVariantList: btnVariant[];
};
export default AsBtnConsts;
